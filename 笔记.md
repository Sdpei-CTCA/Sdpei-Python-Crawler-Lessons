# Python爬虫笔记  

## 第一章：爬虫基础

### 一、爬虫定义
是一种按照一定的规则，自动地抓取万维网信息的程序或脚本。

### 二、通用爬虫工作原理
1. **开始** -> 初始 URL
2. -> 获取网页
3. -> 提取新的 URL 放入 URL 队列
4. -> **判断停止条件**
   - 满足 -> **结束**
   - 不满足 -> **重新获取网页**

### 三、搜索引擎的工作流程
1. **信息采集**：通过爬虫将互联网中的 URL 放入文档库中
2. **索引与检索**：编制索引，提供检索服务
3. **Web 接口**：提供用户接口向用户提供搜索服务

### 四、聚焦爬虫工作原理
1. **开始** -> 初始 URL
2. -> 获取网页
3. -> 提取新的 URL 放入 URL 队列
4. -> **根据分析算法评价网页及 URL**
5. -> **判断停止条件**
   - 满足 -> **结束**
   - 不满足 -> **重新获取网页**

### 五、爬虫爬取网页的详细流程
*(此处待补充详细流程)*

### 六、通用爬虫中网页的分类
- 已下载网页
- 已过期网页
- 待下载网页
- 可知网页
- 不可知网页

### 七、通用爬虫相关网站文件
- `Robots.txt`
- `sitemap.xml`

### 八、选择 Python 做爬虫的原因
1. 爬取网页本身的接口丰富
2. 网页爬取后的处理方便
3. 开发效率高
4. 上手快

---

## 第二章：常用工具与库

### 一、Beautiful Soup
Beautiful Soup 是一个用于解析和提取网页数据的 Python 库。它基于底层解析器（如 lxml、html.parser）把 HTML/XML 转成可遍历、可搜索、可修改的解析树。

#### 1. 特点
- **易用性**：提供简单的查找与选择器接口（`find`, `find_all`, `select`）。
- **容错性**：能处理不规范的 HTML。
- **灵活性**：支持多种解析器，速度与容错可按需选择。

#### 2. 安装
```bash
pip install beautifulsoup4 lxml
```

#### 3. 基本使用示例
```python
from bs4 import BeautifulSoup

html = '<ul><li class="item"><a href="/1">标题1</a></li></ul>'
soup = BeautifulSoup(html, 'lxml')

# 使用 CSS 选择器提取
links = soup.select('li.item > a')
for link in links:
    print(link.get_text(), link['href'])
```

### 二、NumPy 数值计算
NumPy 是 Python 科学计算的基础库，提供高性能的多维数组对象及相关操作。

#### 1. 数组创建
```python
import numpy as np
# 从列表创建
a = np.array([0, 1, 2, 3])
# 创建等差数组
c = np.arange(10, 20, 2)
# 创建随机数组
r = np.random.rand(2, 3) # 2x3 矩阵
```

#### 2. 数组操作
- **Reshape**: `d = np.arange(12).reshape(3, 4)`
- **统计**: `np.max(a)`, `np.min(a)`, `np.average(a)`, `np.var(a)`
- **向量化运算**: 相比 Python 循环，NumPy 的数组运算（如 `a**2 + b**2`）速度更快。

---

## 第三章：Pandas 数据分析
Pandas 基于 NumPy 构建，提供了高效的数据操作工具，核心数据结构是 Series 和 DataFrame。

### 一、数据结构
- **Series**: 一维数组，带标签。
- **DataFrame**: 二维表格，带行索引和列索引。
```python
import pandas as pd
# 创建 DataFrame
data = {'日期': ['1月', '2月'], '销量': [100, 200]}
df = pd.DataFrame(data)
```

### 二、数据读取与导出
- **读取**: `pd.read_excel('file.xlsx')`, `pd.read_csv('file.csv')`
- **导出**: `df.to_excel('new.xlsx')`

### 三、数据选择与筛选
- **列选择**: `df['列名']`, `df[['列1', '列2']]`
- **行选择**: `df.loc[行标签]`, `df.iloc[行位置]`
- **条件筛选**: `df[df['销量'] > 150]`
- **多条件**: `&` (与), `|` (或), `~` (非)

### 四、数据清洗
- **缺失值**: `df.dropna()` (删除), `df.fillna(0)` (填充)
- **重复值**: `df.drop_duplicates()`
- **类型转换**: `df.astype('float')`
- **重命名**: `df.rename(columns={'old': 'new'})`
- **排序**: `df.sort_values(by='列名', ascending=False)`

### 五、数据聚合与转换
- **分组**: `df.groupby('列名').sum()`
- **透视表**: `pd.pivot_table(df, index=..., columns=..., values=...)`
- **映射**: `df['列'].map(dict_or_func)`
- **应用函数**: `df.apply(func, axis=0/1)`

---

## 第四章：Matplotlib 数据可视化
Matplotlib 是 Python 最常用的绘图库。

### 一、基本绘图
```python
import matplotlib.pyplot as plt
import numpy as np

data = np.arange(10)
plt.plot(data)
plt.show()
```

### 二、子图与对象接口
推荐使用面向对象的方式绘图。
```python
fig, ax = plt.subplots() # 创建画布和坐标轴
ax.plot(np.random.randn(100).cumsum(), 'k--', label='Trend')
ax.set_title("Title")
ax.set_xlabel("X Label")
ax.legend() # 显示图例
```

### 三、常用图表元素
- **子图网格**: `fig.add_subplot(2, 2, 1)` 或 `plt.subplots(2, 2)`
- **标注**: `ax.annotate('Text', xy=(x, y), ...)`
- **图形对象**: `Rectangle`, `Circle`, `Polygon` (通过 `ax.add_patch` 添加)
- **保存**: `fig.savefig("fig.png", dpi=400)`

---

## 第五章：urllib 库详解 (Ch4)
urllib 是 Python 内置的 HTTP 请求库。

### 一、四大模块
1. `urllib.request`: 请求模块
2. `urllib.error`: 异常处理模块
3. `urllib.parse`: URL 解析模块
4. `urllib.robotparser`: robots.txt 解析模块

### 二、基本使用
```python
import urllib.request
# 快速爬取
response = urllib.request.urlopen('https://www.example.com')
html = response.read().decode('UTF-8')
print(response.getcode()) # 状态码
```

### 三、高级功能
- **URL 编码**: `urllib.parse.urlencode(data)`
- **请求伪装 (Headers)**:
  ```python
  headers = {"User-Agent": "Mozilla/5.0 ..."}
  req = urllib.request.Request(url, headers=headers)
  resp = urllib.request.urlopen(req)
  ```
- **代理设置**: 使用 `ProxyHandler` 和 `build_opener`。
- **异常处理**: `URLError`, `HTTPError`。

---

## 第六章：数据解析技术 (Ch5)

### 一、正则表达式 (Re)
用于字符串匹配和提取。
- **基本匹配**: `re.match`, `re.search`, `re.findall`
- **编译**: `pattern = re.compile(r"...")`
- **分组**: `match.group(1)`
- **替换**: `re.sub(pattern, repl, text)`

### 二、XPath 与 lxml
XPath 是一种在 XML 文档中查找信息的语言。
- **lxml 使用**:
  ```python
  from lxml import etree
  html = etree.parse('file.html')
  result = html.xpath('//li/a/text()') # 获取 li 下 a 标签的文本
  ```

### 三、Beautiful Soup (进阶)
- **查找方法**:
  - `find_all(name, attrs, recursive, text, limit)`
  - `select("css_selector")` (如 `soup.select('div.content > a')`)

### 四、JSONPath 与 JSON
用于解析 JSON 数据。
- **JSON 模块**: `json.loads()` (str -> obj), `json.dumps()` (obj -> str)
- **JSONPath**: 类似于 XPath 的 JSON 查询语言。
  ```python
  import jsonpath
  city_list = jsonpath.jsonpath(json_obj, '$..name') # 递归查找所有 name 字段
  ```